<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows plain HTTP only to local-dev backend hosts, since the backend has no
  TLS certificate for local development. Everything else still requires
  HTTPS. Update/remove the domains below if the backend gains TLS or moves
  to a real host — do not widen this to cleartextTrafficPermitted for all
  domains.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
