ENVIRONMENT=local
DEBUG=true

# Postgres
POSTGRES_USER=we_wealth
POSTGRES_PASSWORD=we_wealth
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=we_wealth

DATABASE_URL=postgresql+psycopg://we_wealth:we_wealth@localhost:5432/we_wealth

# JWT
JWT_SECRET_KEY=change-me-to-a-long-random-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS
BACKEND_CORS_ORIGINS=["http://localhost:3000"]
